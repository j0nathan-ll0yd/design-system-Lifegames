---
// Showcase entry (`/`) — renders the representative (baseline) dashboard from the
// canonical `@lifegames/fixtures` source. This is a demo surface only: full-page
// visual regression lives on the web consumer and widget coverage lives in
// Storybook, so the showcase carries no visual-test suite of its own.
import DashboardShowcase from '../components/DashboardShowcase.astro';
import { getShowcaseData } from '../lib/showcase-data';

const data = getShowcaseData();
---

<DashboardShowcase data={data} />
