---
import NotFound from '../widgets/identity/NotFound.astro';
import type { NotFoundProps } from '../widgets/identity/NotFound.types';

const props = Astro.props as NotFoundProps;
---
<NotFound {...props} />
