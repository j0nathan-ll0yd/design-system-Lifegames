// Generated by packages/copy/scripts/build.ts — do not edit manually.
// Source of truth: packages/copy/src/permissions.en-US.json (rich) → derived flat schema.
// Re-run: pnpm -F @lifegames/copy build

/**
 * Rich authoring schema for the permissions slice of @lifegames/copy. Every Info.plist privacy-usage-description string the iOS app and watch present at permission-request time, keyed per-target: the app and watch targets intentionally use different wording (the watch omits app-only context). Each leaf is a CopyString carrying authoring context in _meta. Source of truth is ios-LifegamesPortal/LifePortal/project.yml (the NS*UsageDescription Info.plist properties on the LifePortal app target and the LifePortalWatch target). The copy build derives a FLAT schema from this one (stripping _meta) for all consumer codegen (TS/Zod/Swift). Strings are authored in ICU MessageFormat 1 syntax (static passthrough — no interpolation here). Stored in natural case. The $defs block is byte-identical to schema/identity.schema.json, schema/widgets.schema.json, and schema/a11y.schema.json; the $defs are inlined per schema file (no cross-file $ref), which the flat-schema derivation requires.
 */
export interface Permissions {
  health: PermHealth;
  locationWhenInUse: PermLocation;
  locationAlways: PermLocationAlways;
  motion: PermMotion;
}
/**
 * NSHealthShareUsageDescription — shown when the app/watch requests HealthKit read access. The app variant adds 'to display metrics'; the watch variant only syncs.
 */
export interface PermHealth {
  app: string;
  watch: string;
}
/**
 * NSLocationWhenInUseUsageDescription — shown when the app/watch requests foreground location access. The app variant tracks movement history; the watch variant only displays current position.
 */
export interface PermLocation {
  app: string;
  watch: string;
}
/**
 * NSLocationAlwaysAndWhenInUseUsageDescription — shown when the app requests background ('always') location access. App-only: the watch target has no always-on location usage description.
 */
export interface PermLocationAlways {
  app: string;
}
/**
 * NSMotionUsageDescription — shown when the app requests Core Motion access for arrival/departure detection. App-only: the watch target has no motion usage description.
 */
export interface PermMotion {
  app: string;
}
