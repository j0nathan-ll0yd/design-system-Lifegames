// Generated by packages/copy/scripts/build.ts — do not edit manually.
// Source of truth: packages/copy/src/llm.en-US.json (rich) → derived flat schema.
// Re-run: pnpm -F @j0nathan-ll0yd/copy build

import { z } from 'zod';

export const llmSchema = z
  .object({
    txt: z
      .object({
        titleHeading: z.string().regex(new RegExp('^[^#\\s]')),
        aboutHeading: z.string().regex(new RegExp('^[^#\\s]')),
        aboutBody: z.string(),
        linkSite: z
          .object({
            label: z
              .string()
              .regex(new RegExp('^(?![-*+]\\s)[^\\[\\]]+$'))
              .describe(
                "The link's visible text, authored in ICU MessageFormat 1 syntax. Carries no list-bullet affix and no markdown link brackets.",
              ),
            url: z
              .string()
              .regex(new RegExp('^[^()\\s]+$'))
              .describe(
                'The link target, authored in ICU MessageFormat 1 syntax — a {placeholder} base the consumer substitutes, plus any literal path. Carries no surrounding markdown parentheses and no whitespace.',
              ),
            notes: z
              .string()
              .regex(new RegExp('^[^:\\s]'))
              .describe("Optional prose describing the target. Carries no leading ': ' separator.")
              .optional(),
          })
          .strict(),
        linkGithub: z
          .object({
            label: z
              .string()
              .regex(new RegExp('^(?![-*+]\\s)[^\\[\\]]+$'))
              .describe(
                "The link's visible text, authored in ICU MessageFormat 1 syntax. Carries no list-bullet affix and no markdown link brackets.",
              ),
            url: z
              .string()
              .regex(new RegExp('^[^()\\s]+$'))
              .describe(
                'The link target, authored in ICU MessageFormat 1 syntax — a {placeholder} base the consumer substitutes, plus any literal path. Carries no surrounding markdown parentheses and no whitespace.',
              ),
            notes: z
              .string()
              .regex(new RegExp('^[^:\\s]'))
              .describe("Optional prose describing the target. Carries no leading ': ' separator.")
              .optional(),
          })
          .strict(),
        linkLinkedin: z
          .object({
            label: z
              .string()
              .regex(new RegExp('^(?![-*+]\\s)[^\\[\\]]+$'))
              .describe(
                "The link's visible text, authored in ICU MessageFormat 1 syntax. Carries no list-bullet affix and no markdown link brackets.",
              ),
            url: z
              .string()
              .regex(new RegExp('^[^()\\s]+$'))
              .describe(
                'The link target, authored in ICU MessageFormat 1 syntax — a {placeholder} base the consumer substitutes, plus any literal path. Carries no surrounding markdown parentheses and no whitespace.',
              ),
            notes: z
              .string()
              .regex(new RegExp('^[^:\\s]'))
              .describe("Optional prose describing the target. Carries no leading ': ' separator.")
              .optional(),
          })
          .strict(),
        liveHeading: z.string().regex(new RegExp('^[^#\\s]')),
        liveBody: z.string(),
        liveFullDump: z
          .object({
            label: z
              .string()
              .regex(new RegExp('^(?![-*+]\\s)[^\\[\\]]+$'))
              .describe(
                "The link's visible text, authored in ICU MessageFormat 1 syntax. Carries no list-bullet affix and no markdown link brackets.",
              ),
            url: z
              .string()
              .regex(new RegExp('^[^()\\s]+$'))
              .describe(
                'The link target, authored in ICU MessageFormat 1 syntax — a {placeholder} base the consumer substitutes, plus any literal path. Carries no surrounding markdown parentheses and no whitespace.',
              ),
            notes: z
              .string()
              .regex(new RegExp('^[^:\\s]'))
              .describe("Optional prose describing the target. Carries no leading ': ' separator.")
              .optional(),
          })
          .strict(),
        liveIndexAlias: z
          .object({
            label: z
              .string()
              .regex(new RegExp('^(?![-*+]\\s)[^\\[\\]]+$'))
              .describe(
                "The link's visible text, authored in ICU MessageFormat 1 syntax. Carries no list-bullet affix and no markdown link brackets.",
              ),
            url: z
              .string()
              .regex(new RegExp('^[^()\\s]+$'))
              .describe(
                'The link target, authored in ICU MessageFormat 1 syntax — a {placeholder} base the consumer substitutes, plus any literal path. Carries no surrounding markdown parentheses and no whitespace.',
              ),
            notes: z
              .string()
              .regex(new RegExp('^[^:\\s]'))
              .describe("Optional prose describing the target. Carries no leading ': ' separator.")
              .optional(),
          })
          .strict(),
        canonicalHeading: z.string().regex(new RegExp('^[^#\\s]')),
        canonicalBody: z.string(),
        endpointHealth: z
          .object({
            label: z
              .string()
              .regex(new RegExp('^(?![-*+]\\s)[^\\[\\]]+$'))
              .describe(
                "The link's visible text, authored in ICU MessageFormat 1 syntax. Carries no list-bullet affix and no markdown link brackets.",
              ),
            url: z
              .string()
              .regex(new RegExp('^[^()\\s]+$'))
              .describe(
                'The link target, authored in ICU MessageFormat 1 syntax — a {placeholder} base the consumer substitutes, plus any literal path. Carries no surrounding markdown parentheses and no whitespace.',
              ),
            notes: z
              .string()
              .regex(new RegExp('^[^:\\s]'))
              .describe("Optional prose describing the target. Carries no leading ': ' separator.")
              .optional(),
          })
          .strict(),
        endpointSleep: z
          .object({
            label: z
              .string()
              .regex(new RegExp('^(?![-*+]\\s)[^\\[\\]]+$'))
              .describe(
                "The link's visible text, authored in ICU MessageFormat 1 syntax. Carries no list-bullet affix and no markdown link brackets.",
              ),
            url: z
              .string()
              .regex(new RegExp('^[^()\\s]+$'))
              .describe(
                'The link target, authored in ICU MessageFormat 1 syntax — a {placeholder} base the consumer substitutes, plus any literal path. Carries no surrounding markdown parentheses and no whitespace.',
              ),
            notes: z
              .string()
              .regex(new RegExp('^[^:\\s]'))
              .describe("Optional prose describing the target. Carries no leading ': ' separator.")
              .optional(),
          })
          .strict(),
        endpointFocus: z
          .object({
            label: z
              .string()
              .regex(new RegExp('^(?![-*+]\\s)[^\\[\\]]+$'))
              .describe(
                "The link's visible text, authored in ICU MessageFormat 1 syntax. Carries no list-bullet affix and no markdown link brackets.",
              ),
            url: z
              .string()
              .regex(new RegExp('^[^()\\s]+$'))
              .describe(
                'The link target, authored in ICU MessageFormat 1 syntax — a {placeholder} base the consumer substitutes, plus any literal path. Carries no surrounding markdown parentheses and no whitespace.',
              ),
            notes: z
              .string()
              .regex(new RegExp('^[^:\\s]'))
              .describe("Optional prose describing the target. Carries no leading ': ' separator.")
              .optional(),
          })
          .strict(),
        endpointGithubEvents: z
          .object({
            label: z
              .string()
              .regex(new RegExp('^(?![-*+]\\s)[^\\[\\]]+$'))
              .describe(
                "The link's visible text, authored in ICU MessageFormat 1 syntax. Carries no list-bullet affix and no markdown link brackets.",
              ),
            url: z
              .string()
              .regex(new RegExp('^[^()\\s]+$'))
              .describe(
                'The link target, authored in ICU MessageFormat 1 syntax — a {placeholder} base the consumer substitutes, plus any literal path. Carries no surrounding markdown parentheses and no whitespace.',
              ),
            notes: z
              .string()
              .regex(new RegExp('^[^:\\s]'))
              .describe("Optional prose describing the target. Carries no leading ': ' separator.")
              .optional(),
          })
          .strict(),
        endpointStarred: z
          .object({
            label: z
              .string()
              .regex(new RegExp('^(?![-*+]\\s)[^\\[\\]]+$'))
              .describe(
                "The link's visible text, authored in ICU MessageFormat 1 syntax. Carries no list-bullet affix and no markdown link brackets.",
              ),
            url: z
              .string()
              .regex(new RegExp('^[^()\\s]+$'))
              .describe(
                'The link target, authored in ICU MessageFormat 1 syntax — a {placeholder} base the consumer substitutes, plus any literal path. Carries no surrounding markdown parentheses and no whitespace.',
              ),
            notes: z
              .string()
              .regex(new RegExp('^[^:\\s]'))
              .describe("Optional prose describing the target. Carries no leading ': ' separator.")
              .optional(),
          })
          .strict(),
        endpointBooks: z
          .object({
            label: z
              .string()
              .regex(new RegExp('^(?![-*+]\\s)[^\\[\\]]+$'))
              .describe(
                "The link's visible text, authored in ICU MessageFormat 1 syntax. Carries no list-bullet affix and no markdown link brackets.",
              ),
            url: z
              .string()
              .regex(new RegExp('^[^()\\s]+$'))
              .describe(
                'The link target, authored in ICU MessageFormat 1 syntax — a {placeholder} base the consumer substitutes, plus any literal path. Carries no surrounding markdown parentheses and no whitespace.',
              ),
            notes: z
              .string()
              .regex(new RegExp('^[^:\\s]'))
              .describe("Optional prose describing the target. Carries no leading ': ' separator.")
              .optional(),
          })
          .strict(),
        endpointArticles: z
          .object({
            label: z
              .string()
              .regex(new RegExp('^(?![-*+]\\s)[^\\[\\]]+$'))
              .describe(
                "The link's visible text, authored in ICU MessageFormat 1 syntax. Carries no list-bullet affix and no markdown link brackets.",
              ),
            url: z
              .string()
              .regex(new RegExp('^[^()\\s]+$'))
              .describe(
                'The link target, authored in ICU MessageFormat 1 syntax — a {placeholder} base the consumer substitutes, plus any literal path. Carries no surrounding markdown parentheses and no whitespace.',
              ),
            notes: z
              .string()
              .regex(new RegExp('^[^:\\s]'))
              .describe("Optional prose describing the target. Carries no leading ': ' separator.")
              .optional(),
          })
          .strict(),
        endpointTheatre: z
          .object({
            label: z
              .string()
              .regex(new RegExp('^(?![-*+]\\s)[^\\[\\]]+$'))
              .describe(
                "The link's visible text, authored in ICU MessageFormat 1 syntax. Carries no list-bullet affix and no markdown link brackets.",
              ),
            url: z
              .string()
              .regex(new RegExp('^[^()\\s]+$'))
              .describe(
                'The link target, authored in ICU MessageFormat 1 syntax — a {placeholder} base the consumer substitutes, plus any literal path. Carries no surrounding markdown parentheses and no whitespace.',
              ),
            notes: z
              .string()
              .regex(new RegExp('^[^:\\s]'))
              .describe("Optional prose describing the target. Carries no leading ': ' separator.")
              .optional(),
          })
          .strict(),
        endpointWorkouts: z
          .object({
            label: z
              .string()
              .regex(new RegExp('^(?![-*+]\\s)[^\\[\\]]+$'))
              .describe(
                "The link's visible text, authored in ICU MessageFormat 1 syntax. Carries no list-bullet affix and no markdown link brackets.",
              ),
            url: z
              .string()
              .regex(new RegExp('^[^()\\s]+$'))
              .describe(
                'The link target, authored in ICU MessageFormat 1 syntax — a {placeholder} base the consumer substitutes, plus any literal path. Carries no surrounding markdown parentheses and no whitespace.',
              ),
            notes: z
              .string()
              .regex(new RegExp('^[^:\\s]'))
              .describe("Optional prose describing the target. Carries no leading ': ' separator.")
              .optional(),
          })
          .strict(),
        technologyHeading: z.string().regex(new RegExp('^[^#\\s]')),
        technologyFramework: z.string(),
        technologyHosting: z.string(),
        technologyLiveData: z.string(),
        technologyDesign: z.string(),
        expertiseHeading: z.string().regex(new RegExp('^[^#\\s]')),
        optionalHeading: z.string().regex(new RegExp('^[^#\\s]')),
        optionalBody: z.string(),
        wikiSpec: z
          .object({
            label: z
              .string()
              .regex(new RegExp('^(?![-*+]\\s)[^\\[\\]]+$'))
              .describe(
                "The link's visible text, authored in ICU MessageFormat 1 syntax. Carries no list-bullet affix and no markdown link brackets.",
              ),
            url: z
              .string()
              .regex(new RegExp('^[^()\\s]+$'))
              .describe(
                'The link target, authored in ICU MessageFormat 1 syntax — a {placeholder} base the consumer substitutes, plus any literal path. Carries no surrounding markdown parentheses and no whitespace.',
              ),
            notes: z
              .string()
              .regex(new RegExp('^[^:\\s]'))
              .describe("Optional prose describing the target. Carries no leading ': ' separator.")
              .optional(),
          })
          .strict(),
        wikiArchitecture: z
          .object({
            label: z
              .string()
              .regex(new RegExp('^(?![-*+]\\s)[^\\[\\]]+$'))
              .describe(
                "The link's visible text, authored in ICU MessageFormat 1 syntax. Carries no list-bullet affix and no markdown link brackets.",
              ),
            url: z
              .string()
              .regex(new RegExp('^[^()\\s]+$'))
              .describe(
                'The link target, authored in ICU MessageFormat 1 syntax — a {placeholder} base the consumer substitutes, plus any literal path. Carries no surrounding markdown parentheses and no whitespace.',
              ),
            notes: z
              .string()
              .regex(new RegExp('^[^:\\s]'))
              .describe("Optional prose describing the target. Carries no leading ': ' separator.")
              .optional(),
          })
          .strict(),
        wikiBrand: z
          .object({
            label: z
              .string()
              .regex(new RegExp('^(?![-*+]\\s)[^\\[\\]]+$'))
              .describe(
                "The link's visible text, authored in ICU MessageFormat 1 syntax. Carries no list-bullet affix and no markdown link brackets.",
              ),
            url: z
              .string()
              .regex(new RegExp('^[^()\\s]+$'))
              .describe(
                'The link target, authored in ICU MessageFormat 1 syntax — a {placeholder} base the consumer substitutes, plus any literal path. Carries no surrounding markdown parentheses and no whitespace.',
              ),
            notes: z
              .string()
              .regex(new RegExp('^[^:\\s]'))
              .describe("Optional prose describing the target. Carries no leading ': ' separator.")
              .optional(),
          })
          .strict(),
      })
      .strict()
      .describe(
        "Prose strings from llms-txt.eta — the short LLM discovery index (/llms.txt). Section headings (CopyHeading — bare text), links (CopyLink — {label, url, notes}), list-label prefixes, explanatory paragraphs, the technology block, and the JSON-endpoint descriptions. The consumer's codec owns every markdown affix.",
      ),
    full: z
      .object({
        systemFraming: z.string(),
        titleHeading: z.string(),
        intro: z.string(),
        metaLastUpdated: z.string(),
        metaGenerated: z.string(),
        metaSource: z.string(),
        contentsHeading: z.string(),
        contentsProfile: z.string(),
        contentsBody: z.string(),
        contentsMind: z.string(),
        contentsSystem: z.string(),
        contentsStreams: z.string(),
        profileHeading: z.string(),
        profileNameLine: z.string(),
        profileBio: z.string(),
        profileLocation: z.string(),
        profileExperience: z.string(),
        profilePhilosophy: z.string(),
        profileCurrentFocus: z.string(),
        profileSkills: z.string(),
        profileInterests: z.string(),
        expertiseHeading: z.string(),
        linksHeading: z.string(),
        linkSite: z.string(),
        linkGithub: z.string(),
        linkLinkedin: z.string(),
        bodyHeading: z.string(),
        bodyDataAsOf: z.string(),
        bodyAggregateNote: z.string(),
        cardioHeading: z.string(),
        cardioEmpty: z.string(),
        cardioColumns: z.string(),
        cardioRestingLabel: z.string(),
        cardioHrvLabel: z.string(),
        sleepHeading: z.string(),
        sleepEmpty: z.string(),
        sleepColumns: z.string(),
        sleepTotalLabel: z.string(),
        sleepEfficiencyLabel: z.string(),
        sleepDeepLabel: z.string(),
        sleepRemLabel: z.string(),
        activityHeading: z.string(),
        workoutsHeading: z.string(),
        workoutsEmpty: z.string(),
        workoutsActivityTypes: z.string(),
        workoutsAvgDuration: z.string(),
        workoutsAvgEnergy: z.string(),
        hydrationHeading: z.string(),
        hydrationWater: z.string(),
        hydrationCaffeine: z.string(),
        mindHeading: z.string(),
        githubHeading: z.string(),
        githubDataAsOf: z.string(),
        githubEmpty: z.string(),
        githubActiveRepos: z.string(),
        githubRecentEvents: z.string(),
        githubStarredCount: z.string(),
        githubRecentCommitsHeading: z.string(),
        githubRecentPrsHeading: z.string(),
        starredHeading: z.string(),
        starredDataAsOf: z.string(),
        starredEmpty: z.string(),
        starredSummary: z.string(),
        starredTopics: z.string(),
        bookshelfHeading: z.string(),
        bookshelfDataAsOf: z.string(),
        bookshelfEmpty: z.string(),
        bookshelfReadingHeading: z.string(),
        bookshelfByAuthor: z.string(),
        bookshelfNotesLabel: z.string(),
        bookshelfIsbnLabel: z.string(),
        bookshelfUpNextHeading: z.string(),
        bookshelfFinishedHeading: z.string(),
        bookshelfStatsHeading: z.string(),
        bookshelfStatBooksRead: z.string(),
        bookshelfStatReading: z.string(),
        bookshelfStatUpNext: z.string(),
        bookshelfStatAvgRating: z.string(),
        articlesHeading: z.string(),
        articlesDataAsOf: z.string(),
        articlesEmpty: z.string(),
        articlesColumns: z.string(),
        theatreHeading: z.string(),
        theatreDataAsOf: z.string(),
        theatreEmpty: z.string(),
        theatreByLine: z.string(),
        theatreSource: z.string(),
        systemHeading: z.string(),
        freshnessHeading: z.string(),
        freshnessColumns: z.string(),
        freshnessNote: z.string(),
        streamsHeading: z.string(),
        streamsBody: z.string(),
        streamHealth: z.string(),
        streamSleep: z.string(),
        streamFocus: z.string(),
        streamGithubEvents: z.string(),
        streamStarred: z.string(),
        streamBooks: z.string(),
        streamArticles: z.string(),
        streamTheatre: z.string(),
        streamWorkouts: z.string(),
      })
      .strict()
      .describe(
        'Prose strings from llms-full.eta — the complete LLM data dump (/llms-full.txt + /index.md). The <SYSTEM> framing line, headings, blockquote intro, meta labels, table column headers, the _No … available._ fallbacks, list-label prefixes, and explanatory paragraphs.',
      ),
    dashboard: z
      .object({
        alternateLinkMarkdown: z.string(),
        alternateLinkPlain: z.string(),
        datasetDescription: z.string(),
      })
      .strict()
      .describe(
        'LLM-facing strings used in the web dashboard HTML (Dashboard.astro) — alternate link titles and JSON-LD Dataset prose.',
      ),
    mcp: z
      .object({
        serverDescription: z.string(),
        agentSkillDescription: z.string(),
        toolGetProfile: z.string(),
        toolGetDataSources: z.string(),
        toolGetCurrentReading: z.string(),
        toolGetTechStack: z.string(),
        stackFramework: z.string(),
        stackHosting: z.string(),
        stackLiveData: z.string(),
        stackDesign: z.string(),
        stackFont: z.string(),
        dsHealthName: z.string(),
        dsHealthDesc: z.string(),
        dsSleepName: z.string(),
        dsSleepDesc: z.string(),
        dsFocusName: z.string(),
        dsFocusDesc: z.string(),
        dsGithubEventsName: z.string(),
        dsGithubEventsDesc: z.string(),
        dsStarredReposName: z.string(),
        dsStarredReposDesc: z.string(),
        dsBooksName: z.string(),
        dsBooksDesc: z.string(),
        dsArticlesName: z.string(),
        dsArticlesDesc: z.string(),
        dsTheatreReviewsName: z.string(),
        dsTheatreReviewsDesc: z.string(),
        dsWorkoutsName: z.string(),
        dsWorkoutsDesc: z.string(),
      })
      .strict()
      .describe(
        'Prose strings for the WebMCP client (public/js/webmcp.js), MCP server-card (.well-known/mcp/server-card.json), and agent-skills index (.well-known/agent-skills/index.json). Generated by scripts/generate-webmcp.mjs at prebuild. Includes tool descriptions, tech-stack values, and data-source name/description pairs (shared across webmcp and server-card).',
      ),
    agentDiscovery: z
      .object({
        agentCardDescription: z.string(),
        agentCardSkillName: z.string(),
        agentCardSkillDescription: z.string(),
        agentCardSkillExamples: z.array(z.string()),
        aiCatalogMcpName: z.string(),
        aiCatalogMcpDescription: z.string(),
        aiCatalogMcpQueries: z.array(z.string()),
        aiCatalogSkillsName: z.string(),
        aiCatalogSkillsDescription: z.string(),
        aiCatalogSkillsQueries: z.array(z.string()),
        aiCatalogA2aName: z.string(),
        aiCatalogA2aDescription: z.string(),
        aiCatalogA2aQueries: z.array(z.string()),
      })
      .strict()
      .describe(
        'Prose strings for the A2A agent-card (.well-known/agent-card.json) and AI catalog (.well-known/ai-catalog.json). Generated by scripts/generate-webmcp.mjs at prebuild. Machine-facing discovery metadata only — no URLs, identifiers, or structural values.',
      ),
  })
  .strict()
  .describe(
    "Rich authoring schema for the llm slice of @j0nathan-ll0yd/copy. Every hardcoded English-prose string the LLM-facing surfaces render: the backend LLM-content Eta templates (src/lib/llm-content/templates/llms-txt.eta and llms-full.eta) that the ComposeLlmContent Lambda renders to the public /llms.txt, /llms-full.txt, and /index.md, plus the web dashboard HTML and the .well-known discovery documents that the web repo's scripts/generate-webmcp.mjs emits at prebuild. Five groups: `txt` (title LlmTxt, from llms-txt.eta — the short discovery index), `full` (title LlmFull, from llms-full.eta — the complete data dump), `dashboard` (title LlmDashboard), `mcp` (title LlmMcp), and `agentDiscovery` (title LlmAgentDiscovery); each group's own description names its source. Every leaf carries authoring context in _meta whose usage cites that source file:line. The copy build derives a FLAT schema from this one (stripping _meta) for all consumer codegen (TS/Zod/Swift). Values in the Eta-sourced groups (`txt`, `full`) are stored as the literal template line text with each Eta interpolation tag (<%= it.X %>) replaced by an ICU MessageFormat 1 placeholder ({x}), and all literal markdown markers/punctuation preserved verbatim — so the rendered template output stays byte-identical. EXCEPTION, the `txt` group's headings and links: they ship as FIELDS, not as rendered markdown (atlas decision 0128 P3a). Heading leaves are CopyHeading (bare text, no '#' affix) and link leaves are CopyLink ({label, url, notes}, no '- [](): ' affixes), because the consumer's llms.txt codec models exactly that structure and used to regex-parse the affixes straight back off before re-adding them. The `full` group keeps its rendered form on purpose: its consumer renders it through Eta verbatim and never re-parses it. Casing is the source's natural case. Every object group has a UNIQUE title (Llm*) so quicktype-derived Swift struct names never collide across namespaces. The $defs block is byte-identical to schema/identity.schema.json, schema/widgets.schema.json, schema/a11y.schema.json, schema/app.schema.json, schema/permissions.schema.json, and schema/errors.schema.json; the $defs are inlined per schema file (no cross-file $ref), which the flat-schema derivation requires.",
  );
