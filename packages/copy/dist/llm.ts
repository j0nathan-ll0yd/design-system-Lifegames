// Generated by packages/copy/scripts/build.ts — do not edit manually.
// Source of truth: packages/copy/src/llm.en-US.json (rich) → derived flat schema.
// Re-run: pnpm -F @lifegames/copy build

/**
 * Rich authoring schema for the llm slice of @lifegames/copy. Every hardcoded English-prose string in the backend LLM-content Eta templates (src/lib/llm-content/templates/llms-txt.eta and llms-full.eta) that the ComposeLlmContent Lambda renders to the public /llms.txt, /llms-full.txt, and /index.md surfaces. Two groups: `txt` (title LlmTxt, from llms-txt.eta — the short discovery index) and `full` (title LlmFull, from llms-full.eta — the complete data dump). Each leaf is a CopyString carrying authoring context in _meta with usage citing the template file:line. The copy build derives a FLAT schema from this one (stripping _meta) for all consumer codegen (TS/Zod/Swift). Values are stored as the literal template line text with each Eta interpolation tag (<%= it.X %>) replaced by an ICU MessageFormat 1 placeholder ({x}), and all literal markdown markers/punctuation preserved verbatim — so the rendered template output stays byte-identical. Casing is the source's natural case. Every object group has a UNIQUE title (Llm*) so quicktype-derived Swift struct names never collide across namespaces. The $defs block is byte-identical to schema/identity.schema.json, schema/widgets.schema.json, schema/a11y.schema.json, schema/app.schema.json, and schema/permissions.schema.json; the $defs are inlined per schema file (no cross-file $ref), which the flat-schema derivation requires.
 */
export interface Llm {
  txt: LlmTxt;
  full: LlmFull;
  dashboard: LlmDashboard;
  agentDiscovery: LlmAgentDiscovery;
}
/**
 * Prose strings from llms-txt.eta — the short LLM discovery index (/llms.txt). Section headings, list-label prefixes, explanatory paragraphs, link descriptions, technology block, and the JSON-endpoint descriptions.
 */
export interface LlmTxt {
  titleHeading: string;
  aboutHeading: string;
  aboutBody: string;
  linkSite: string;
  linkGithub: string;
  linkLinkedin: string;
  liveHeading: string;
  liveBody: string;
  liveFullDump: string;
  liveIndexAlias: string;
  canonicalHeading: string;
  canonicalBody: string;
  endpointHealth: string;
  endpointSleep: string;
  endpointFocus: string;
  endpointGithubEvents: string;
  endpointStarred: string;
  endpointBooks: string;
  endpointArticles: string;
  endpointTheatre: string;
  endpointWorkouts: string;
  endpointLocation: string;
  technologyHeading: string;
  technologyFramework: string;
  technologyHosting: string;
  technologyLiveData: string;
  technologyDesign: string;
  expertiseHeading: string;
  optionalHeading: string;
  optionalBody: string;
  wikiSpec: string;
  wikiArchitecture: string;
  wikiBrand: string;
}
/**
 * Prose strings from llms-full.eta — the complete LLM data dump (/llms-full.txt + /index.md). The <SYSTEM> framing line, headings, blockquote intro, meta labels, table column headers, the _No … available._ fallbacks, list-label prefixes, and explanatory paragraphs.
 */
export interface LlmFull {
  systemFraming: string;
  titleHeading: string;
  intro: string;
  metaLastUpdated: string;
  metaGenerated: string;
  metaSource: string;
  contentsHeading: string;
  contentsProfile: string;
  contentsBody: string;
  contentsMind: string;
  contentsSystem: string;
  contentsStreams: string;
  profileHeading: string;
  profileNameLine: string;
  profileBio: string;
  profileLocation: string;
  profileExperience: string;
  profilePhilosophy: string;
  profileCurrentFocus: string;
  profileSkills: string;
  profileInterests: string;
  expertiseHeading: string;
  linksHeading: string;
  linkSite: string;
  linkGithub: string;
  linkLinkedin: string;
  bodyHeading: string;
  bodyDataAsOf: string;
  bodyAggregateNote: string;
  cardioHeading: string;
  cardioEmpty: string;
  cardioColumns: string;
  cardioRestingLabel: string;
  cardioHrvLabel: string;
  sleepHeading: string;
  sleepEmpty: string;
  sleepColumns: string;
  sleepTotalLabel: string;
  sleepEfficiencyLabel: string;
  sleepDeepLabel: string;
  sleepRemLabel: string;
  activityHeading: string;
  workoutsHeading: string;
  workoutsEmpty: string;
  workoutsActivityTypes: string;
  workoutsAvgDuration: string;
  workoutsAvgEnergy: string;
  hydrationHeading: string;
  hydrationWater: string;
  hydrationCaffeine: string;
  mindHeading: string;
  githubHeading: string;
  githubDataAsOf: string;
  githubEmpty: string;
  githubActiveRepos: string;
  githubRecentEvents: string;
  githubStarredCount: string;
  githubRecentCommitsHeading: string;
  githubRecentPrsHeading: string;
  starredHeading: string;
  starredDataAsOf: string;
  starredEmpty: string;
  starredSummary: string;
  starredTopics: string;
  bookshelfHeading: string;
  bookshelfDataAsOf: string;
  bookshelfEmpty: string;
  bookshelfReadingHeading: string;
  bookshelfByAuthor: string;
  bookshelfNotesLabel: string;
  bookshelfIsbnLabel: string;
  bookshelfUpNextHeading: string;
  bookshelfFinishedHeading: string;
  bookshelfStatsHeading: string;
  bookshelfStatBooksRead: string;
  bookshelfStatReading: string;
  bookshelfStatUpNext: string;
  bookshelfStatAvgRating: string;
  articlesHeading: string;
  articlesDataAsOf: string;
  articlesEmpty: string;
  articlesColumns: string;
  theatreHeading: string;
  theatreDataAsOf: string;
  theatreEmpty: string;
  theatreByLine: string;
  theatreSource: string;
  systemHeading: string;
  freshnessHeading: string;
  freshnessColumns: string;
  freshnessNote: string;
  streamsHeading: string;
  streamsBody: string;
  streamHealth: string;
  streamSleep: string;
  streamFocus: string;
  streamGithubEvents: string;
  streamStarred: string;
  streamBooks: string;
  streamArticles: string;
  streamTheatre: string;
  streamWorkouts: string;
  streamLocation: string;
}
/**
 * LLM-facing strings used in the web dashboard HTML (Dashboard.astro) — alternate link titles and JSON-LD Dataset prose.
 */
export interface LlmDashboard {
  alternateLinkMarkdown: string;
  alternateLinkPlain: string;
  datasetDescription: string;
}
/**
 * Prose strings for the A2A agent-card (.well-known/agent-card.json) and AI catalog (.well-known/ai-catalog.json). Generated by scripts/generate-webmcp.mjs at prebuild. Machine-facing discovery metadata only — no URLs, identifiers, or structural values.
 */
export interface LlmAgentDiscovery {
  agentCardDescription: string;
  agentCardSkillName: string;
  agentCardSkillDescription: string;
  agentCardSkillExamples: string[];
  aiCatalogMcpName: string;
  aiCatalogMcpDescription: string;
  aiCatalogMcpQueries: string[];
  aiCatalogSkillsName: string;
  aiCatalogSkillsDescription: string;
  aiCatalogSkillsQueries: string[];
  aiCatalogA2aName: string;
  aiCatalogA2aDescription: string;
  aiCatalogA2aQueries: string[];
}
