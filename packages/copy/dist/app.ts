// Generated by packages/copy/scripts/build.ts — do not edit manually.
// Source of truth: packages/copy/src/app.en-US.json (rich) → derived flat schema.
// Re-run: pnpm -F @lifegames/copy build

/**
 * Rich authoring schema for the app slice of @lifegames/copy. Every customer-facing UI string in the iOS/watchOS LifePortal app — navigation titles, tab labels, section headers, buttons, alerts, field labels, placeholders, and interpolated templates — is a CopyString leaf carrying authoring context in _meta. Source of truth is ios-LifegamesPortal/LifePortal/Packages/LifePortalFeatures/Sources/** (verified file:line in each leaf's _meta.usage). The copy build derives a FLAT schema from this one (stripping _meta) for all consumer codegen (TS/Zod/Swift). Strings are authored in ICU MessageFormat 1 syntax; interpolated templates carry their ICU placeholders ({count}, {percent}, {relative}, …) and ICU plural syntax where the source pluralizes — consumers keep their native plural logic. Stored in natural case: the app uppercases section headers / buttons at the call site via .textCase / .kerning, so stored values are natural case ('Sync Now', not 'SYNC NOW'). Every object group has a UNIQUE title (App* / Perm-free App-prefixed) so quicktype-derived Swift struct names never collide across namespaces. The $defs block is byte-identical to schema/identity.schema.json, schema/widgets.schema.json, and schema/a11y.schema.json; the $defs are inlined per schema file (no cross-file $ref), which the flat-schema derivation requires.
 */
export interface App {
  nav: AppNav;
  tab: AppTab;
  common: AppCommon;
  home: AppHome;
  settings: AppSettings;
  savedPlaces: AppSavedPlaces;
  addPlace: AppAddPlace;
  health: AppHealth;
  sleep: AppSleep;
  location: AppLocation;
  bookshelf: AppBookshelf;
  watch: AppWatch;
  sections: AppSections;
}
/**
 * Navigation-bar titles for the app's primary screens (SwiftUI .navigationTitle).
 */
export interface AppNav {
  home: string;
  health: string;
  settings: string;
  location: string;
  journey: string;
  activityMap: string;
  topPlaces: string;
  bookshelf: string;
  diagnostics: string;
  savedPlaces: string;
}
/**
 * Bottom tab-bar labels (TabView Label text).
 */
export interface AppTab {
  home: string;
  health: string;
  location: string;
  books: string;
  settings: string;
}
/**
 * Shared button/action labels reused across many screens (alert buttons, toolbar actions).
 */
export interface AppCommon {
  cancel: string;
  delete: string;
  clear: string;
  ok: string;
  done: string;
  save: string;
  add: string;
}
/**
 * Home dashboard strings — loading state, health/location summary cards, and the sync-status banner.
 */
export interface AppHome {
  loading: string;
  stepsUnit: string;
  goalProgress: string;
  visitsToday: string;
  unsynced: string;
  sectionHealth: string;
  sectionLocation: string;
  noLocation: string;
  tracking: string;
  notTracking: string;
  notSynced: string;
  syncStatus: string;
}
/**
 * Settings screen — section headers, server/data/diagnostics/about rows, buttons, and destructive-action alerts.
 */
export interface AppSettings {
  sectionLocation: string;
  sectionServer: string;
  sectionData: string;
  sectionDiagnostics: string;
  sectionAbout: string;
  savedPlacesTitle: string;
  savedPlacesSubtitle: string;
  apiBaseUrl: string;
  apiBaseUrlPlaceholder: string;
  apiToken: string;
  apiTokenPlaceholder: string;
  totalVisits: string;
  unsyncedVisits: string;
  lastHealthSync: string;
  fullResync: string;
  deleteAll: string;
  watchLogEntries: string;
  fileSize: string;
  diagEmpty: string;
  recentEntries: string;
  shareLog: string;
  clearLogs: string;
  reload: string;
  aboutName: string;
  aboutTitle: string;
  aboutVersion: string;
  aboutBuild: string;
  aboutPlatform: string;
  alerts: AppSettingsAlerts;
}
/**
 * Settings destructive-action confirmation alerts (delete-all, full re-sync, clear-logs).
 */
export interface AppSettingsAlerts {
  deleteAllTitle: string;
  deleteAllMessage: string;
  resyncTitle: string;
  resyncMessage: string;
  resyncConfirm: string;
  clearLogsTitle: string;
  clearLogsMessage: string;
}
/**
 * Saved Places list — empty state and per-row radius label.
 */
export interface AppSavedPlaces {
  emptyTitle: string;
  emptyBody: string;
  addHome: string;
  radius: string;
}
/**
 * Add Saved Place sheet — header, field labels, placeholders, radius template, and coordinate tip.
 */
export interface AppAddPlace {
  title: string;
  sectionNewLocation: string;
  fieldName: string;
  fieldNamePlaceholder: string;
  fieldCategory: string;
  fieldLatitude: string;
  fieldLongitude: string;
  fieldRadius: string;
  tip: string;
}
/**
 * Health dashboard — section headers, workouts, HealthKit authorization, sync, and data-mode toggle.
 */
export interface AppHealth {
  sectionHeart: string;
  sectionMovement: string;
  sectionSleep: string;
  sectionWorkouts: string;
  sectionData: string;
  sectionDataMode: string;
  todaysWorkouts: string;
  recoveryDay: string;
  noWorkouts: string;
  workoutDuration: string;
  workoutCalories: string;
  sectionHealthkit: string;
  authorization: string;
  authorized: string;
  lastSync: string;
  syncing: string;
  syncNow: string;
  useRealHR: string;
  realActive: string;
  simActive: string;
}
/**
 * Sleep-score entry sheet — title, prompt, placeholder, buttons, and a11y labels for the add/edit affordance.
 */
export interface AppSleep {
  title: string;
  prompt: string;
  placeholder: string;
  save: string;
  deleteScore: string;
  cancel: string;
  a11yAdd: string;
  a11yEdit: string;
}
/**
 * Location feature — grouped by sub-screen (journey, visit correction, place rename, top places, heat map, today stats, week chart, now-at hero, merge, current-location ribbon, visit duration).
 */
export interface AppLocation {
  journey: AppLocationJourney;
  visitCorrection: AppLocationVisitCorrection;
  placeRename: AppLocationPlaceRename;
  topPlaces: AppLocationTopPlaces;
  heatMap: AppLocationHeatMap;
  todayStats: AppLocationTodayStats;
  weekChart: AppLocationWeekChart;
  nowAt: AppLocationNowAt;
  merge: AppLocationMerge;
  ribbon: AppLocationRibbon;
  visitDuration: AppLocationVisitDuration;
}
/**
 * Journey timeline screen — empty state, per-visit actions, and the delete-visit alert.
 */
export interface AppLocationJourney {
  viewYesterday: string;
  mapUnavailable: string;
  placesVisited: string;
  emptyTitle: string;
  emptyMessage: string;
  actionCorrect: string;
  actionSavePlace: string;
  actionMerge: string;
  actionDelete: string;
  visitFallback: string;
  unknown: string;
  deleteVisitTitle: string;
  deleteVisitMessage: string;
}
/**
 * Visit Correction sheet — header, tab hints, time pickers, and confirm button.
 */
export interface AppLocationVisitCorrection {
  title: string;
  unknownVisit: string;
  dragPin: string;
  searchPlaceholder: string;
  adjustTimes: string;
  arrived: string;
  departed: string;
  confirming: string;
  confirm: string;
  pinnedLocation: string;
}
/**
 * Place Rename sheet — title, subtitle template, success template, fields.
 */
export interface AppLocationPlaceRename {
  title: string;
  subtitle: string;
  result: string;
  done: string;
  newName: string;
  newNamePlaceholder: string;
  category: string;
}
/**
 * Top Places card + full screen — header, counts, empty state, sort options, and per-place actions.
 */
export interface AppLocationTopPlaces {
  header: string;
  seeAll: string;
  empty: string;
  uniqueLocations: string;
  emptyTitle: string;
  emptyMessage: string;
  sort: string;
  sortMostVisited: string;
  sortMostRecent: string;
  recentVisits: string;
  rename: string;
  lastVisit: string;
  visitsUnit: string;
}
/**
 * Activity Map card + full screen — header, legend, totals, and scale template.
 */
export interface AppLocationHeatMap {
  header: string;
  seeMonth: string;
  legendLess: string;
  legendMore: string;
  totalVisits: string;
  scale: string;
}
/**
 * Today stats row — the three stat-cell labels.
 */
export interface AppLocationTodayStats {
  visits: string;
  timeOut: string;
  distance: string;
}
/**
 * This-week bar chart — header and total-visits template.
 */
export interface AppLocationWeekChart {
  header: string;
  totalVisits: string;
}
/**
 * Now-At hero card — contextual header labels and fallbacks for current/last location.
 */
export interface AppLocationNowAt {
  atHome: string;
  currentlyHere: string;
  lastVisited: string;
  lastKnown: string;
  location: string;
  homeFallback: string;
  unknownLocation: string;
  waiting: string;
}
/**
 * Merge Visits sheet — header, keep/absorb/result labels, and merged-samples template.
 */
export interface AppLocationMerge {
  title: string;
  subtitle: string;
  combining: string;
  keep: string;
  absorb: string;
  result: string;
  samplesMerged: string;
}
/**
 * Current-location ribbon — staleness prefix, proximity template, and exploring fallback.
 */
export interface AppLocationRibbon {
  lastSeenPrefix: string;
  near: string;
  exploring: string;
}
/**
 * Visit-duration fallbacks — in-progress label for an ongoing (not-yet-departed) visit.
 */
export interface AppLocationVisitDuration {
  inProgress: string;
}
/**
 * Bookshelf feature — list sections, dashboard tile, status labels, plus the add/edit book screens and bookshelf alerts.
 */
export interface AppBookshelf {
  navTitle: string;
  pages: string;
  sectionUpNext: string;
  sectionPending: string;
  sectionRecentlyFinished: string;
  currentlyReading: string;
  pageOf: string;
  enriching: string;
  tileHeader: string;
  tileComplete: string;
  tileBooks: string;
  tileReading: string;
  statusPending: string;
  statusReading: string;
  statusUpNext: string;
  statusFinished: string;
  addBook: AppBookshelfAddBook;
  editBook: AppBookshelfEditBook;
  alerts: AppBookshelfAlerts;
}
/**
 * Add Book search screen — title, search placeholder, empty prompt, ASIN template, add button.
 */
export interface AppBookshelfAddBook {
  title: string;
  searchPlaceholder: string;
  emptyPrompt: string;
  asin: string;
  add: string;
}
/**
 * Edit Book screen — title, buttons, placeholders, section headers, and metadata field labels.
 */
export interface AppBookshelfEditBook {
  title: string;
  save: string;
  delete: string;
  notesPlaceholder: string;
  descriptionPlaceholder: string;
  page: string;
  of: string;
  sectionDetails: string;
  sectionDescription: string;
  sectionPublication: string;
  sectionCategories: string;
  sectionStatus: string;
  sectionProgress: string;
  sectionRating: string;
  sectionNotes: string;
  fieldSeries: string;
  fieldSeriesPlaceholder: string;
  fieldSeriesNum: string;
  fieldSeriesTotal: string;
  fieldDate: string;
  fieldDatePlaceholder: string;
  fieldYear: string;
  fieldGenres: string;
  fieldGenresPlaceholder: string;
}
/**
 * Bookshelf error-alert titles (load/add/update/delete failures).
 */
export interface AppBookshelfAlerts {
  failedToLoad: string;
  failedToAddBook: string;
  failedToUpdate: string;
  failedToDelete: string;
}
/**
 * watchOS app + sync-status complication — diagnostics chrome, sync action labels, and complication strings.
 */
export interface AppWatch {
  diagTitle: string;
  diagA11yLabel: string;
  diagA11yHint: string;
  transferSent: string;
  openIphone: string;
  authorize: string;
  sync: string;
  syncFailed: string;
  complicationNotSynced: string;
  complicationSyncAgo: string;
  complicationDisplayName: string;
  complicationDescription: string;
}
/**
 * Dashboard triptych column header labels used on the web dashboard (index.astro).
 */
export interface AppSections {
  body: string;
  mind: string;
}
