// Generated by scripts/codegen.ts — do not edit manually.
// Source: authored/media-profile.schema.json

/**
 * An OfflineMediaDownloader account profile: the signed-in user plus aggregate library metrics, as shown on the profile screen. Fixture pool for OMD profile previews (S98). All identity values are synthetic (see SCRUBBING.md).
 */
export interface MediaProfile {
  user: MediaUser;
  metrics: MediaMetrics;
}
/**
 * Signed-in user identity as delivered by Sign in with Apple registration.
 */
export interface MediaUser {
  /**
   * Account email address (synthetic).
   */
  email: string;
  /**
   * Given name (synthetic).
   */
  firstName: string;
  /**
   * Family name (synthetic).
   */
  lastName: string;
  /**
   * Opaque account identifier.
   */
  identifier: string;
}
/**
 * Aggregate library metrics computed from local persistence.
 */
export interface MediaMetrics {
  /**
   * Number of files downloaded to the device.
   */
  downloadCount: number;
  /**
   * Total on-device storage used by downloads, in bytes.
   */
  totalStorageBytes: number;
  /**
   * Total playback sessions across the library.
   */
  playCount: number;
}
