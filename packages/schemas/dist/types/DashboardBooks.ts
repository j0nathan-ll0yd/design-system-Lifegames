// Generated by scripts/codegen.ts — do not edit manually.
// Source: authored/dashboard-books.schema.json

/**
 * Books dashboard data consumed by the books widget. The books array is a simplified display projection of LP books-export data with DS-added fields (isbn, link, progress). bookMeta, statusLabels, and stats are DS-owned additions with no LP equivalent.
 */
export interface DashboardBooks {
  /**
   * List of books projected from LP books-export, augmented with DS display fields. Field set is a subset of LP's books items plus DS-specific additions (isbn, link, progress).
   */
  books: {
    /**
     * Book title.
     */
    title: string;
    /**
     * Author full name.
     */
    author: string;
    /**
     * ISBN-13 (or Kindle ASIN when no ISBN exists) used as a stable key into bookMeta.
     */
    isbn?: string;
    /**
     * Amazon ASIN used for affiliate link construction and as an alternate key into bookMeta.
     */
    asin: string;
    /**
     * Affiliate or direct purchase URL.
     */
    link?: string;
    /**
     * Reading status key. Must match a key in statusLabels.
     */
    status: 'next' | 'in_progress' | 'completed';
    /**
     * User rating 1–5 stars, or null if unrated.
     */
    rating?: number | null;
    /**
     * Reading progress as a percentage (0–100). 0 for unstarted, 100 for completed.
     */
    progress?: number;
  }[];
  /**
   * Supplementary metadata keyed by isbn (or ASIN for Kindle-only titles). Provides series, genre, page count, year, and description for each book in the books array.
   */
  bookMeta: {
    [k: string]: {
      /**
       * Series name if part of a series, null otherwise.
       */
      seriesName?: string | null;
      /**
       * Position within the series, null if standalone.
       */
      seriesNumber?: number | null;
      /**
       * Total number of books in the series, null if standalone or unknown.
       */
      seriesTotal?: number | null;
      /**
       * Total page count.
       */
      pages: number;
      /**
       * Genre tags.
       */
      genres: string[];
      /**
       * Publication year.
       */
      year: number;
      /**
       * Short plot or synopsis description used in the book detail panel.
       */
      desc: string;
    };
  };
  /**
   * Human-readable display labels keyed by status value. Maps status enum values to UI strings.
   */
  statusLabels: {
    /**
     * Label for books queued to read next.
     */
    next: string;
    /**
     * Label for the book currently being read.
     */
    in_progress: string;
    /**
     * Label for recently finished books.
     */
    completed: string;
  };
  /**
   * Aggregate reading statistics computed by the DS pipeline.
   */
  stats: {
    /**
     * Total number of books with completed status.
     */
    totalRead: number;
    /**
     * Number of books currently in-progress.
     */
    currentlyReading: number;
    /**
     * Number of books queued with next status.
     */
    upNext: number;
    /**
     * Average star rating across all rated books.
     */
    avgRating: number;
    /**
     * Total books started or completed in the current calendar year.
     */
    booksThisYear: number;
  };
}
