// Generated by scripts/codegen.ts — do not edit manually.
// Source: authored/dashboard-health.schema.json

/**
 * Daily health dashboard data consumed by the health widget, combining Apple Health quantities (composed from the LP health-export shape) with sleep phases, workout records, reference ranges, derived metrics, and hydration.
 */
export interface DashboardHealth {
  /**
   * ISO 8601 date string for which the health data applies.
   */
  date: string;
  quantities: Quantities;
  /**
   * Sleep phase breakdown keyed by phase name, each with a duration in seconds.
   */
  sleep: {
    awake?: {
      /**
       * Duration in seconds spent awake.
       */
      seconds: number;
    };
    core?: {
      /**
       * Duration in seconds in core sleep.
       */
      seconds: number;
    };
    deep?: {
      /**
       * Duration in seconds in deep sleep.
       */
      seconds: number;
    };
    rem?: {
      /**
       * Duration in seconds in REM sleep.
       */
      seconds: number;
    };
  };
  /**
   * List of workout records for the day. Empty array when no workouts were logged.
   */
  workouts: {
    /**
     * Human-readable workout type.
     */
    activity_type: string;
    /**
     * Duration of the workout in seconds.
     */
    duration: number;
    /**
     * Active energy burned in kilocalories.
     */
    energy_burned: number;
    /**
     * Distance covered in meters. 0 for non-distance activities.
     */
    distance: number;
  }[];
  /**
   * Clinical and goal reference ranges used by the widget to color-code metrics.
   */
  ranges?: {
    heartRate?: {
      low: number;
      /**
       * @minItems 2
       * @maxItems 2
       */
      normal: [number, number];
      high: number;
      unit: string;
    };
    restingHeartRate?: {
      /**
       * @minItems 2
       * @maxItems 2
       */
      excellent: [number, number];
      /**
       * @minItems 2
       * @maxItems 2
       */
      good: [number, number];
      /**
       * @minItems 2
       * @maxItems 2
       */
      fair: [number, number];
      /**
       * @minItems 2
       * @maxItems 2
       */
      poor: [number, number];
    };
    hrvSDNN?: {
      /**
       * @minItems 2
       * @maxItems 2
       */
      poor: [number, number];
      /**
       * @minItems 2
       * @maxItems 2
       */
      fair: [number, number];
      /**
       * @minItems 2
       * @maxItems 2
       */
      good: [number, number];
      /**
       * @minItems 2
       * @maxItems 2
       */
      excellent: [number, number];
    };
    respiratoryRate?: {
      /**
       * @minItems 2
       * @maxItems 2
       */
      low: [number, number];
      /**
       * @minItems 2
       * @maxItems 2
       */
      normal: [number, number];
      /**
       * @minItems 2
       * @maxItems 2
       */
      high: [number, number];
    };
    stepCount?: {
      /**
       * Daily step count goal.
       */
      goal: number;
    };
    activeEnergy?: {
      /**
       * Daily active energy goal in kilocalories.
       */
      goal: number;
    };
    exerciseTime?: {
      /**
       * Daily exercise time goal in minutes.
       */
      goal: number;
    };
    standTime?: {
      /**
       * Daily stand time goal in minutes.
       */
      goal: number;
    };
    sleep?: {
      /**
       * Target sleep duration in hours.
       */
      targetHours: number;
      /**
       * Ideal deep-sleep percentage range [min, max].
       *
       * @minItems 2
       * @maxItems 2
       */
      deepPctIdeal: [number, number];
      /**
       * Ideal REM percentage range [min, max].
       *
       * @minItems 2
       * @maxItems 2
       */
      remPctIdeal: [number, number];
      /**
       * Minimum sleep efficiency percentage considered good.
       */
      efficiencyGood: number;
    };
  };
  /**
   * Pre-computed derived metrics used directly by the widget to avoid runtime calculation.
   */
  derived?: {
    /**
     * Total sleep duration in seconds (sum of all phases).
     */
    totalSleepSeconds: number;
    /**
     * Total time in bed in seconds including awake periods.
     */
    timeInBedSeconds: number;
    /**
     * Sleep efficiency as a percentage (totalSleep / timeInBed * 100).
     */
    sleepEfficiency: number;
    /**
     * Total calories burned (active + basal) in kilocalories.
     */
    totalCalories: number;
    /**
     * Total walking/running distance in kilometres.
     */
    distanceKm: number;
    /**
     * Deep sleep as a percentage of total sleep.
     */
    deepPct: number;
    /**
     * REM sleep as a percentage of total sleep.
     */
    remPct: number;
    /**
     * Core sleep as a percentage of total sleep.
     */
    corePct: number;
  };
  /**
   * Composite sleep quality score (0-100).
   */
  sleepScore?: number;
  /**
   * Human-readable total sleep duration string.
   */
  sleepDurationFormatted?: string;
  /**
   * Human-readable formatted duration strings for each sleep phase.
   */
  sleepPhaseFormatted?: {
    deep: string;
    rem: string;
    core: string;
    awake: string;
  };
  /**
   * Representative recent workout records for display when live data is unavailable.
   */
  sampleWorkouts?: {
    /**
     * Human-readable workout type.
     */
    activity_type: string;
    /**
     * Duration in seconds.
     */
    duration: number;
    /**
     * Active energy burned in kilocalories.
     */
    energy_burned: number;
    /**
     * Distance in meters.
     */
    distance: number;
  }[];
  /**
   * Daily hydration and caffeine intake values and reference ranges.
   */
  hydration?: {
    /**
     * Water consumed in fluid ounces.
     */
    waterOz: number;
    /**
     * Caffeine consumed in milligrams.
     */
    caffeineMg: number;
    /**
     * Maximum daily water intake goal in fluid ounces.
     */
    waterMax: number;
    /**
     * Maximum recommended caffeine in milligrams.
     */
    caffeineMax: number;
    /**
     * Lower bound of healthy water intake range in fluid ounces.
     */
    waterRangeLo: number;
    /**
     * Upper bound of healthy water intake range in fluid ounces.
     */
    waterRangeHi: number;
    /**
     * Lower bound of moderate caffeine range in milligrams.
     */
    caffeineRangeLo: number;
    /**
     * Upper bound of moderate caffeine range in milligrams.
     */
    caffeineRangeHi: number;
  };
}
/**
 * Map of HealthKit quantity identifiers to their latest value and unit. Structure matches LP health-export quantities.
 */
export interface Quantities {
  [k: string]: {
    value: number;
    unit: string;
  };
}
