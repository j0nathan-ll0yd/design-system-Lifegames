// Generated by scripts/codegen.ts — do not edit manually.
// Source: authored/profile.schema.json

/**
 * Line role: 'prompt' renders a shell prompt, 'output' renders response text, 'blank' renders an empty line, 'cursor' renders the blinking cursor.
 */
export type TerminalLineType = 'prompt' | 'output' | 'blank' | 'cursor';

/**
 * Personal profile data displayed in the portfolio hero and terminal widget, including contact links, bio copy, and the animated terminal line sequence.
 */
export interface Profile {
  /**
   * Full display name.
   */
  name: string;
  /**
   * Current job title or professional role.
   */
  title: string;
  /**
   * Human-readable city/region string.
   */
  location: string;
  /**
   * Geographic coordinates as [latitude, longitude].
   *
   * @minItems 2
   * @maxItems 2
   */
  coordinates?: [number, number];
  /**
   * Full URL to LinkedIn profile.
   */
  linkedin?: string;
  /**
   * Full URL to GitHub profile.
   */
  github?: string;
  /**
   * Short biography sentence used in the about section.
   */
  bio: string;
  /**
   * Short punchy tagline displayed as a subtitle.
   */
  tagline: string;
  /**
   * Relative or absolute URL to the avatar image.
   */
  avatar: string;
  /**
   * Ordered sequence of lines rendered in the animated terminal widget.
   */
  terminalLines: {
    type: TerminalLineType;
    /**
     * Raw text content of the line. Empty string for blank and cursor lines.
     */
    text: string;
  }[];
}
