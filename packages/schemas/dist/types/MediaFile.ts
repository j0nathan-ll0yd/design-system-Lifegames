// Generated by scripts/codegen.ts — do not edit manually.
// Source: authored/media-file.schema.json

/**
 * Download lifecycle status. Wire values match the OMD FileStatus enum raw values ('pending' is lowercase because push notifications send it lowercase).
 */
export type MediaFileStatus = 'pending' | 'Queued' | 'Downloading' | 'Downloaded' | 'Failed';

/**
 * A single downloadable media file as delivered by the OfflineMediaDownloader backend (API Gateway wire shape). Fixture pool for OMD previews (S98); numeric fields are raw wire values (bytes, seconds), never display-formatted strings.
 */
export interface MediaFile {
  /**
   * Stable backend identifier for the file.
   */
  fileId: string;
  /**
   * S3 object key / local filename.
   */
  key: string;
  /**
   * Publish date in YYYYMMDD format (API) or ISO-8601 (push notifications).
   */
  publishDate?: string;
  /**
   * File size in bytes.
   */
  size?: number;
  /**
   * Download URL for the media asset.
   */
  url?: string;
  /**
   * Human-readable media title.
   */
  title?: string;
  /**
   * Longer descriptive text shown on the detail screen.
   */
  description?: string;
  /**
   * Display name of the content author or channel.
   */
  authorName?: string;
  /**
   * Author account handle.
   */
  authorUser?: string;
  /**
   * MIME type of the media asset.
   */
  contentType?: string;
  status?: MediaFileStatus;
  /**
   * Media duration in seconds.
   */
  duration?: number;
  /**
   * Upload date in YYYYMMDD format.
   */
  uploadDate?: string;
  /**
   * Number of views at the source platform.
   */
  viewCount?: number;
  /**
   * URL to the media thumbnail image.
   */
  thumbnailUrl?: string;
}
