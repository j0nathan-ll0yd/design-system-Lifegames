// Type declaration for the plain-.mjs resolver so the tsx-run .ts scripts
// (validate.ts, codegen.ts, generate-contract-lock.ts, verify-contract.ts)
// consume it with proper types instead of implicit `any`.
export const RAW_SCHEMAS_DIR: string;
