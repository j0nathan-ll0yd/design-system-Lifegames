import SwiftUI

/// Forwarding aliases — kept for source compatibility during _Legacy/ transition.
/// After C3 visual approval, this file is deleted and callers migrate to LGColor.*
public extension Color {
    // MARK: - Surface

    static let colorSurfaceBase = LGColor.surfaceBase
    static let colorSurfaceDeep = LGColor.surfaceDeep
    static let colorSurfaceRaised = LGColor.surfaceRaised
    static let colorSurfaceRaisedHover = LGColor.surfaceRaisedHover
    static let colorSurfaceInset = LGColor.surfaceInset
    static let surfaceBase = LGColor.surfaceBase
    static let surfaceDeep = LGColor.surfaceDeep
    static let surfaceRaised = LGColor.surfaceRaised
    static let surfaceRaisedHover = LGColor.surfaceRaisedHover
    static let surfaceInset = LGColor.surfaceInset

    // MARK: - Border

    static let colorBorderDefault = LGColor.borderDefault
    static let colorBorderSubtle = LGColor.borderSubtle
    static let colorBorderStrong = LGColor.borderStrong
    static let colorBorderInteractive = LGColor.borderInteractive
    static let borderDefault = LGColor.borderDefault
    static let borderSubtle = LGColor.borderSubtle
    static let borderStrong = LGColor.borderStrong
    static let borderInteractive = LGColor.borderInteractive

    // MARK: - Text

    static let colorTextTitle = LGColor.textTitle
    static let colorTextPrimary = LGColor.textPrimary
    static let colorTextMuted = LGColor.textMuted
    static let colorTextSubtle = LGColor.textSubtle
    static let colorTextDisabled = LGColor.textDisabled
    static let textTitle = LGColor.textTitle
    static let textPrimary = LGColor.textPrimary
    static let textMuted = LGColor.textMuted
    static let textSubtle = LGColor.textSubtle
    static let textDisabled = LGColor.textDisabled

    // MARK: - Accent

    static let colorAccentPink = LGColor.accentPink
    static let colorAccentBlue = LGColor.accentBlue
    static let colorAccentGreen = LGColor.accentGreen
    static let colorAccentAmber = LGColor.accentAmber
    static let colorAccentPurple = LGColor.accentPurple
    static let colorAccentRed = LGColor.accentRed
    static let colorAccentCyan = LGColor.accentCyan
    static let colorAccentOrange = LGColor.accentOrange
    static let colorAccentIndigo = LGColor.accentIndigo
    static let colorAccentDefault = LGColor.accentDefault
    static let accentPink = LGColor.accentPink
    static let accentBlue = LGColor.accentBlue
    static let accentGreen = LGColor.accentGreen
    static let accentAmber = LGColor.accentAmber
    static let accentPurple = LGColor.accentPurple
    static let accentRed = LGColor.accentRed
    static let accentCyan = LGColor.accentCyan
    static let accentOrange = LGColor.accentOrange
    static let accentIndigo = LGColor.accentIndigo
    static let accentDefault = LGColor.accentDefault

    // MARK: - Health

    static let colorHealthRed = LGColor.healthRed
    static let colorHealthGreen = LGColor.healthGreen
    static let colorHealthPurple = LGColor.healthPurple
    static let healthRed = LGColor.healthRed
    static let healthGreen = LGColor.healthGreen
    static let healthPurple = LGColor.healthPurple

    // MARK: - Interactive

    static let colorInteractiveDefault = LGColor.interactiveDefault
    static let colorInteractiveHover = LGColor.interactiveHover
    static let interactiveDefault = LGColor.interactiveDefault
    static let interactiveHover = LGColor.interactiveHover
}
