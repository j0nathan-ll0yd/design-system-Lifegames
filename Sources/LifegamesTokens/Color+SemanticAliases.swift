import SwiftUI

public extension Color {
    // MARK: - Surface

    static let surfaceBase = colorSurfaceBase
    static let surfaceDeep = colorSurfaceDeep
    static let surfaceRaised = colorSurfaceRaised
    static let surfaceRaisedHover = colorSurfaceRaisedHover
    static let surfaceInset = colorSurfaceInset

    // MARK: - Border

    static let borderDefault = colorBorderDefault
    static let borderSubtle = colorBorderSubtle
    static let borderStrong = colorBorderStrong
    static let borderInteractive = colorBorderInteractive

    // MARK: - Text

    static let textTitle = colorTextTitle
    static let textPrimary = colorTextPrimary
    static let textMuted = colorTextMuted
    static let textSubtle = colorTextSubtle
    static let textDisabled = colorTextDisabled

    // MARK: - Accent

    static let accentPink = colorAccentPink
    static let accentBlue = colorAccentBlue
    static let accentGreen = colorAccentGreen
    static let accentAmber = colorAccentAmber
    static let accentPurple = colorAccentPurple
    static let accentRed = colorAccentRed
    static let accentCyan = colorAccentCyan
    static let accentOrange = colorAccentOrange
    static let accentIndigo = colorAccentIndigo
    static let accentDefault = colorAccentDefault

    // MARK: - Health

    static let healthRed = colorHealthRed
    static let healthGreen = colorHealthGreen
    static let healthPurple = colorHealthPurple

    // MARK: - Interactive

    static let interactiveDefault = colorInteractiveDefault
    static let interactiveHover = colorInteractiveHover
}
